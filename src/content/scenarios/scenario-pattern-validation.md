---
id: scenario-pattern-validation
title: pattern attribute does not validate contenteditable content
description: The `pattern` attribute, which allows regex-based validation on form inputs, does not work on contenteditable regions. Content cannot be validated against a pattern.
category: other
tags:
  - pattern
  - validation
  - regex
  - firefox
status: draft
---

The `pattern` attribute, which allows regex-based validation on form inputs, does not work on contenteditable regions. Content cannot be validated against a pattern.
