---
import SiteNav from '../../../../components/SiteNav.astro';
import BaseHead from '../../../../components/BaseHead.astro';
import TableOfContents from '../../../../components/TableOfContents.astro';
import DocCodeBlock from '../../../../components/docs/DocCodeBlock.astro';
import DocSection from '../../../../components/docs/DocSection.astro';
import DocNavigation from '../../../../components/docs/DocNavigation.astro';
import '../../../../styles/global.css';

export async function getStaticPaths() {
  return [
    { params: { locale: 'ko' } },
  ];
}

const headings = [
  { depth: 2, slug: 'overview', text: '개요' },
  { depth: 2, slug: 'interface', text: '인터페이스' },
  { depth: 2, slug: 'usage', text: '사용법' },
];

const locale = Astro.params.locale || 'ko';
---

<html lang={locale}>
  <head>
    <BaseHead
      title="Replace Operation – 에디터 아키텍처 – contenteditable.lab"
      description="모델 기반 에디터를 위한 교체 operation"
    />
  </head>
  <body>
    <SiteNav />
    <main class="flex max-w-full m-0 p-0">
      <section class="flex-1 flex max-w-[1120px] mx-auto py-7 px-5 pb-10 gap-6 w-full">
        <div class="flex-1">
          <header class="mb-8">
            <nav class="mb-4 text-sm">
              <a href="/" class="text-text-muted no-underline">홈</a>
              <span class="text-text-faint mx-2">/</span>
              <a href="/editor" class="text-text-muted no-underline">에디터</a>
              <span class="text-text-faint mx-2">/</span>
              <a href="/editor/operations" class="text-text-muted no-underline">Operations</a>
              <span class="text-text-faint mx-2">/</span>
              <span class="text-text-primary">Replace</span>
            </nav>
            <h1 class="text-3xl leading-tight m-0 mb-2">Replace Operation</h1>
            <p class="m-0 text-[0.95rem] text-text-secondary">
              범위의 콘텐츠를 새 콘텐츠로 교체합니다.
            </p>
          </header>

          <article class="rounded-xl border border-border-light bg-bg-surface p-6 px-7 text-[0.9rem] leading-[1.7]">
            <DocSection id="overview" title="개요">
              <p class="m-0 mb-4">
                Replace operation은 단일 operation에서 delete와 insert를 결합합니다. Transaction 그룹화를 위해 delete + insert로 분해할 수 있습니다.
              </p>
            </DocSection>

            <DocSection id="interface" title="인터페이스">
              <DocCodeBlock language="typescript" code={`interface ReplaceOperation extends Operation {
  type: 'replace';
  path: Path;
  length: number; // 교체할 콘텐츠 길이
  content: string | Node; // 새 콘텐츠
  deletedContent?: any; // undo를 위해 삭제된 콘텐츠 저장
}`} />
            </DocSection>

            <DocSection id="usage" title="사용법">
              <DocCodeBlock language="typescript" code={`function replaceSelection(editor: Editor, selection: Selection, newText: string) {
  const operation: ReplaceOperation = {
    type: 'replace',
    path: selection.start,
    length: selection.length,
    content: newText,
    deletedContent: editor.getContent(selection)
  };
  
  editor.applyOperation(operation);
}

// Replace는 delete + insert로 분해 가능
function replaceAsTransaction(editor: Editor, selection: Selection, newText: string) {
  const tx = editor.beginTransaction();
  
  tx.add({
    type: 'deleteContent',
    path: selection.start,
    length: selection.length
  });
  
  tx.add({
    type: 'insertText',
    path: selection.start,
    text: newText
  });
  
  tx.commit();
}`} />
            </DocSection>

            <DocNavigation
              current="/editor/operations/replace"
              pages={[
                { title: 'Operations', href: '/editor/operations', description: 'Operations 개요' },
                { title: 'Insert Text', href: '/editor/operations/insert-text', description: 'Insert text operation' },
                { title: 'Delete Content', href: '/editor/operations/delete-content', description: 'Delete content operation' },
              ]}
            />
          </article>
        </div>
        <TableOfContents headings={headings} />
      </section>
    </main>
  </body>
</html>
