---
import SiteNav from '../../../../components/SiteNav.astro';
import BaseHead from '../../../../components/BaseHead.astro';
import TableOfContents from '../../../../components/TableOfContents.astro';
import DocCodeBlock from '../../../../components/docs/DocCodeBlock.astro';
import DocSection from '../../../../components/docs/DocSection.astro';
import DocNavigation from '../../../../components/docs/DocNavigation.astro';
import '../../../../styles/global.css';

export async function getStaticPaths() {
  return [
    { params: { locale: 'ko' } },
  ];
}

const headings = [
  { depth: 2, slug: 'overview', text: '개요' },
  { depth: 2, slug: 'interface', text: '인터페이스' },
  { depth: 2, slug: 'usage', text: '사용법' },
];

const locale = Astro.params.locale || 'ko';
---

<html lang={locale}>
  <head>
    <BaseHead
      title="Remove Format Operation – 에디터 아키텍처 – contenteditable.lab"
      description="모델 기반 에디터를 위한 서식 제거 operation"
    />
  </head>
  <body>
    <SiteNav />
    <main class="flex max-w-full m-0 p-0">
      <section class="flex-1 flex max-w-[1120px] mx-auto py-7 px-5 pb-10 gap-6 w-full">
        <div class="flex-1">
          <header class="mb-8">
            <nav class="mb-4 text-sm">
              <a href="/" class="text-text-muted no-underline">홈</a>
              <span class="text-text-faint mx-2">/</span>
              <a href="/editor" class="text-text-muted no-underline">에디터</a>
              <span class="text-text-faint mx-2">/</span>
              <a href="/editor/operations" class="text-text-muted no-underline">Operations</a>
              <span class="text-text-faint mx-2">/</span>
              <span class="text-text-primary">Remove Format</span>
            </nav>
            <h1 class="text-3xl leading-tight m-0 mb-2">Remove Format Operation</h1>
            <p class="m-0 text-[0.95rem] text-text-secondary">
              텍스트 범위에서 서식을 제거합니다.
            </p>
          </header>

          <article class="rounded-xl border border-border-light bg-bg-surface p-6 px-7 text-[0.9rem] leading-[1.7]">
            <DocSection id="overview" title="개요">
              <p class="m-0 mb-4">
                Remove format operation은 텍스트 범위에서 서식을 제거하며, undo를 위해 이전 값을 저장합니다.
              </p>
            </DocSection>

            <DocSection id="interface" title="인터페이스">
              <DocCodeBlock language="typescript" code={`interface RemoveFormatOperation extends Operation {
  type: 'removeFormat';
  path: Path;
  length: number;
  format: string;
  previousValue?: any; // undo를 위해 이전 값 저장
}`} />
            </DocSection>

            <DocSection id="usage" title="사용법">
              <DocCodeBlock language="typescript" code={`function removeBold(editor: Editor, selection: Selection) {
  const operation: RemoveFormatOperation = {
    type: 'removeFormat',
    path: selection.start,
    length: selection.length,
    format: 'bold',
    previousValue: editor.hasFormat(selection, 'bold')
  };
  
  editor.applyOperation(operation);
}

// 역: 서식 재적용
const inverse = {
  type: 'applyFormat',
  path: selection.start,
  length: selection.length,
  format: 'bold',
  value: operation.previousValue
};`} />
            </DocSection>

            <DocNavigation
              current="/editor/operations/remove-format"
              pages={[
                { title: 'Operations', href: '/editor/operations', description: 'Operations 개요' },
                { title: 'Apply Format', href: '/editor/operations/apply-format', description: 'Apply format operation' },
              ]}
            />
          </article>
        </div>
        <TableOfContents headings={headings} />
      </section>
    </main>
  </body>
</html>
