---
import SiteNav from '../../../../components/SiteNav.astro';
import BaseHead from '../../../../components/BaseHead.astro';
import TableOfContents from '../../../../components/TableOfContents.astro';
import DocCodeBlock from '../../../../components/docs/DocCodeBlock.astro';
import DocSection from '../../../../components/docs/DocSection.astro';
import DocNavigation from '../../../../components/docs/DocNavigation.astro';
import '../../../../styles/global.css';

export async function getStaticPaths() {
  return [
    { params: { locale: 'ko' } },
  ];
}

const headings = [
  { depth: 2, slug: 'overview', text: '개요' },
  { depth: 2, slug: 'interface', text: '인터페이스' },
  { depth: 2, slug: 'usage', text: '사용법' },
  { depth: 2, slug: 'inverse', text: '역 Operation' },
];

const locale = Astro.params.locale || 'ko';
---

<html lang={locale}>
  <head>
    <BaseHead
      title="Unwrap Operation – 에디터 아키텍처 – contenteditable.lab"
      description="모델 기반 에디터를 위한 풀기 operation"
    />
  </head>
  <body>
    <SiteNav />
    <main class="flex max-w-full m-0 p-0">
      <section class="flex-1 flex max-w-[1120px] mx-auto py-7 px-5 pb-10 gap-6 w-full">
        <div class="flex-1">
          <header class="mb-8">
            <nav class="mb-4 text-sm">
              <a href="/" class="text-text-muted no-underline">홈</a>
              <span class="text-text-faint mx-2">/</span>
              <a href="/editor" class="text-text-muted no-underline">에디터</a>
              <span class="text-text-faint mx-2">/</span>
              <a href="/editor/operations" class="text-text-muted no-underline">Operations</a>
              <span class="text-text-faint mx-2">/</span>
              <span class="text-text-primary">Unwrap</span>
            </nav>
            <h1 class="text-3xl leading-tight m-0 mb-2">Unwrap Operation</h1>
            <p class="m-0 text-[0.95rem] text-text-secondary">
              래퍼 노드를 제거하고 자식을 부모 레벨로 승격합니다.
            </p>
          </header>

          <article class="rounded-xl border border-border-light bg-bg-surface p-6 px-7 text-[0.9rem] leading-[1.7]">
            <DocSection id="overview" title="개요">
              <p class="m-0 mb-4">
                Unwrap operation은 래퍼 노드를 제거하고 자식을 부모 레벨로 승격합니다. 링크 서식 제거 또는 목록 항목을 단락으로 변환할 때 일반적으로 사용됩니다.
              </p>
            </DocSection>

            <DocSection id="interface" title="인터페이스">
              <DocCodeBlock language="typescript" code={`interface UnwrapOperation extends Operation {
  type: 'unwrap';
  path: Path; // 래퍼 노드 경로
  wrapperType?: string; // 풀 래퍼 타입 (검증용)
  preservedWrapper?: Node; // undo를 위해 래퍼 저장
}`} />
            </DocSection>

            <DocSection id="usage" title="사용법">
              <DocCodeBlock language="typescript" code={`// 링크 풀기
function unwrapLink(editor: Editor, linkPath: Path) {
  const linkNode = editor.getNodeAtPath(linkPath);
  
  const operation: UnwrapOperation = {
    type: 'unwrap',
    path: linkPath,
    wrapperType: 'link',
    preservedWrapper: linkNode
  };
  
  editor.applyOperation(operation);
}

// 목록 항목 풀기
function unwrapListItem(editor: Editor, itemPath: Path) {
  const itemNode = editor.getNodeAtPath(itemPath);
  
  const operation: UnwrapOperation = {
    type: 'unwrap',
    path: itemPath,
    wrapperType: 'listItem',
    preservedWrapper: itemNode
  };
  
  editor.applyOperation(operation);
}`} />
            </DocSection>

            <DocSection id="inverse" title="역 Operation">
              <p class="m-0 mb-4">
                Unwrap의 역 operation은 wrap입니다:
              </p>
              <DocCodeBlock language="typescript" code={`function getInverse(operation: UnwrapOperation): WrapOperation {
  return {
    type: 'wrap',
    path: operation.path,
    wrapper: operation.preservedWrapper!
  };
}`} />
            </DocSection>

            <DocNavigation
              current="/editor/operations/unwrap"
              pages={[
                { title: 'Operations', href: '/editor/operations', description: 'Operations 개요' },
                { title: 'Wrap', href: '/editor/operations/wrap', description: 'Wrap operation' },
                { title: 'Delete Content', href: '/editor/operations/delete-content', description: 'Delete content operation' },
              ]}
            />
          </article>
        </div>
        <TableOfContents headings={headings} />
      </section>
    </main>
  </body>
</html>
